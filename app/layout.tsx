import type { Metadata } from 'next'
import { Cormorant_Garamond, Inter } from 'next/font/google'
import './globals.css'

const cormorant = Cormorant_Garamond({
  subsets: ['latin'],
  weight: ['400', '500', '600', '700'],
  variable: '--font-serif',
})

const inter = Inter({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600', '700'],
  variable: '--font-sans',
})

export const metadata: Metadata = {
  title: 'Appearance Aesthetics | Luxury Aesthetic Treatments',
  description: 'Premium aesthetic clinic offering bespoke treatments in a luxury setting. Botox, skin boosters, dermal fillers, and professional beauty treatments.',
  icons: {
    icon: '/favicon.ico',
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <head>
        <meta name="viewport" content="width=device-width, initial-scale=1" />
      </head>
      <body
        className={`${cormorant.variable} ${inter.variable} font-sans bg-luxury-white text-luxury-charcoal antialiased`}
      >
        {children}
      </body>
    </html>
  )
}
